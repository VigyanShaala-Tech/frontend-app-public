import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  'public.page.title': {
    id: 'public.page.title',
    defaultMessage: '{siteName}',
    description: 'Title tag default for public MFE pages',
  },
});

export default messages;
